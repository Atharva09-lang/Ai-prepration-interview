# Development only: base URL of the Express backend (no trailing slash).
# In production the browser calls this app's own origin and next.config.mjs
# proxies /api/* to API_PROXY_TARGET instead (keeps the session cookie first-party).
NEXT_PUBLIC_API_URL=http://localhost:4000
# API_PROXY_TARGET=https://your-api.onrender.com
